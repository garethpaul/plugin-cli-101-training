{
  "name": "@garethpaul/plugin-cli-101-training",
  "description": "Twilio CLI 101 Training",
  "version": "2.0.1",
  "author": "@garethpaul",
  "bugs": "https://github.com/garethpaul/plugin-cli-101-training/issues",
  "dependencies": {
    "@oclif/core": "^1.26.2",
    "@twilio/cli-core": "^8.3.4",
    "chalk": "^4.1.0",
    "clipboardy": "^2.3.0",
    "inquirer": "^8.2.7"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "overrides": {
    "form-data": "4.0.6",
    "js-yaml": "4.2.0"
  },
  "files": [
    "/bin",
    "/oclif.manifest.json",
    "/src"
  ],
  "homepage": "https://github.com/garethpaul/plugin-cli-101-training",
  "keywords": [
    "oclif-plugin"
  ],
  "license": "MIT",
  "oclif": {
    "name": "cli-101-training",
    "commands": "./src/commands",
    "bin": "twilio",
    "repositoryPrefix": "<%- repo %>/blob/<%- version %>/<%- commandPath %>",
    "devPlugins": [
      "@oclif/plugin-help"
    ],
    "topics": {
      "twilio-101-training": {
        "description": "a twilio 101 training"
      }
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/garethpaul/plugin-cli-101-training.git"
  },
  "scripts": {
    "build": "npm run check",
    "check": "node scripts/check-baseline.js",
    "lint": "npm run check",
    "postpack": "node -e \"require('fs').rmSync('oclif.manifest.json', {force: true})\"",
    "prepack": "oclif manifest && oclif readme",
    "test": "npm run check && node test_audit_policy.js && node test_welcome_name_format.js && node test_examples_catalog.js && node test_oclif_commands.js",
    "version": "oclif readme && git add README.md"
  },
  "devDependencies": {
    "oclif": "^4.23.14"
  }
}
